.SS 创建和摧毁
.SS 通用
.SS 创建
所有模块的创建的第一个参数都是自己的双重指针，生命周期由调用者管理。
.SS 摧毁
所有模块的创建的第一个参数都是自己的双重指针，同时所有模块的摧毁函数都只有一个参数。
.SS EventEmitter
.SS 创建
第二个 \f[CR]GHashTable\f[R] 参数是这个发射器的配置。
.SS EventListener
.SS 创建
第二个参数是要监听的动作
.PP
第三个参数是回调指针
.TP
提醒
.PP
该模块 \f[B]不应该\f[R] 被你创建，这是内部的
.SS Event
.SS 创建
第一个参数是该事件的具体动作
.PP
第二个参数是该事件的参数
.PP
第三个参数是该事件来源的发射器
.TP
提醒
.PP
该模块 \f[B]不应该\f[R] 被你创建，这是内部的
